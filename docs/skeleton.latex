\documentclass{article}
$if(geometry)$
\usepackage[$geometry$]{geometry}
$else$
\usepackage{geometry}
$endif$
$if(title)$
\title{$title$}
$endif$
$if(author)$
\author{$for(author)$$author$$sep$ \\ $endfor$}
$endif$
$if(date)$
\date{$date$}
$else$
\date{}
$endif$
\begin{document}
$if(title)$
\maketitle
$endif$

$body$

\end{document}
